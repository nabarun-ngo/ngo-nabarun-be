@nabarun-ngo:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
always-auth=true

# Azure Artifacts (replace org/project/feed):
# @nabarun-ngo:registry=https://pkgs.dev.azure.com/<org>/<project>/_packaging/<feed>/npm/registry/
# //pkgs.dev.azure.com/<org>/<project>/_packaging/<feed>/npm/registry/:_authToken=${NPM_TOKEN}
